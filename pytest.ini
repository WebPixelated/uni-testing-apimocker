[pytest]
addopts =
    --alluredir=allure-results
    --clean-alluredir
    -v
    -p no:warnings

testpaths = tests

markers =
    smoke: быстрые проверки статус-кодов
    regression: полный прогон
    api: только API-тесты (requests)
    ui: только UI-тесты (playwright)